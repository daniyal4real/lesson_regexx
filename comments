match ищет только в начале строки
print(result)

search ищет во всей строке, и останавливается на первом найденом
для получения самого результата
print(result[0])

finall ищет во всей строке полностью и показывает все совпадения
print(result)



split разделяет строку и давляет в список

sub заменяет одну подстроку на другую
result = re.sub("A", "D", s)


fullmatch сравнивает нашу строку с другим выражением
result = re.fullmatch("A", s)